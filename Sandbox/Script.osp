main: () -> i32 = ()
{
    x: i32 = 3;
    y: i32 = 4;
    z: i32 = x + x;
    return z;
}

factorial: (u32) -> u32 = (n)
{
    if (n <= 1)
    {
        return 1;
    }
    else
    {
        return n * factorial(n - 1);
    }
}

value := 5;

reduce := (arr: [i32], fn: (i32 -> i32) -> i32)
{
}

reduce: ([i32], (i32) -> i32) -> i32) = (arr, fn)
{
    
}

main: () -> i32 = ()
{

}